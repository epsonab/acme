spring:
  application:
    name: myrestapp
  cloud:
    kubernetes:
      config:
        name: acme-git-config
      reload:
        enabled: true
      secrets:        name: ldap-secrets
        enable-api: true

        
