spring:
  application:
    name: myrestapp
  cloud:
    kubernetes:
      config:
        name: acme-git-config
      reload:
        enabled: false
      secrets:
        name: ldap-secrets
        paths: /etc/secrets/ldap-secrets

        
